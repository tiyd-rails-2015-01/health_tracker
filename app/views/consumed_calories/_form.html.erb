<%= form_for(@consumed_calory) do |f| %>
  <% if @consumed_calory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consumed_calory.errors.count, "error") %> prohibited this consumed_calory from being saved:</h2>

      <ul>
      <% @consumed_calory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :calorie_amount %><br>
    <%= f.number_field :calorie_amount %>
  </div>
  <div class="field">
    <%= f.label :meal_type %><br>
    <%= f.text_field :meal_type %>
  </div>
  <div class="field">
    <%= f.label :consumed_on %><br>
    <%= f.date_select :consumed_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
