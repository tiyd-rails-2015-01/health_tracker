<h1>Dashboard</h1>
<p>So far today, you have consumed <%= @calories %> calories.</p>
<p>Your steps and exercise together have burned <%= @combined_burn %> calories.</p>
<p>Therefore, your net calorie consumption is <%=@net %>.</p>
<h3>Today, you ate:</h3>
<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Calories</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @todays_foods.each do |food| %>
      <tr>
        <td><%= food.description %></td>
        <td><%= food.calories %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'New Food', new_food_path %>


<h3>Today, you performed the following exercises:</h3>
<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Calories Burned</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @todays_exercise.each do |exercise| %>
      <tr>
        <td><%= exercise.description %></td>
        <td><%= exercise.calories %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'New Exercise', new_exercise_path %>


<h3>Today, you walked this many steps:</h3>
<table>
  <thead>
    <tr>
      <th>Steps</th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <% @todays_steps.each do |step| %>
      <tr>
        <td><%= step.steps %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'New Steps', new_step_path %>

<h3>Today, you weighed <%= @todays_weight %> pounds.</h3>


<%= link_to 'New Weight', new_weight_path %>

<p><h3>Choose a category to view history.</h3></p>
<%= link_to 'Exercises', exercises_path %>

<%= link_to 'Foods', foods_path %>

<%= link_to 'Steps', steps_path %>

<%= link_to 'Weight', weights_path %>
