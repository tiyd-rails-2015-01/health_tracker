<p id="notice"><%= notice %></p>

<p>
  <strong>Date:</strong>
  <%= @step.date %>
</p>

<p>
  <strong>Steps taken:</strong>
  <%= @step.steps_taken %>
</p>

<%= link_to '<button type="button">Edit</button>' .html_safe, edit_step_path(@step) %> |
<%= link_to '<button type="button">Back</button>' .html_safe, steps_path %>
