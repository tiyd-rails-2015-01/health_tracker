<p id="notice"><%= notice %></p>

<h1>Steps</h1>

<table>
  <thead>
    <tr>
      <th>Created at</th>
      <th>Date</th>
      <th>Steps taken</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @steps.each do |step| %>
      <tr>
        <td><%= step.created_at %> |</td>
        <td><%= step.date %> |</td>
        <td><%= step.steps_taken %> </td>
        <td><%= link_to '<button type="button">Show</button>' .html_safe, step %></td>
        <td><%= link_to '<button type="button">Edit</button>' .html_safe, edit_step_path(step) %></td>
        <td><%= link_to '<button type="button">Destroy</button>' .html_safe, step, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<p><strong>Your total steps taken to date: </strong><%= @steps_total %></p>
<br>

<%= link_to '<button type="button">New Steps</button>' .html_safe, new_step_path %>
<%= link_to '<button type="button">New Weight</button>' .html_safe, new_weight_path %>
<%= link_to '<button type="button">New Exercise</button>' .html_safe, new_exercise_path %>
<%= link_to '<button type="button">New Calories</button>' .html_safe, new_calory_path %>
<%= link_to '<button type="button">Home</button>' .html_safe, root_path %>
