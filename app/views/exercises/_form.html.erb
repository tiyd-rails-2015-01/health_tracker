<%= form_for(@exercise) do |f| %>
  <% if @exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :type_of_exercise %><br>
    <%= f.text_field :type_of_exercise %> 
  </div>
  <div class="field">
    <%= f.label :calories_burned %><br>
    <%= f.number_field :calories_burned %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
