<%= form_for(@exercise) do |f| %>
  <% if @exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

      <ul>
      <% @exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :performed_on %><br>
    <%= f.datetime_select :performed_on %>
  </div>
  <div class="field">
    <%= f.label :exercise_type %><br>
    <%= f.text_field :exercise_type %>
  </div>
  <div class="field">
    <%= f.label :exercise_duration %><br>
    <%= f.text_field :exercise_duration %>
  </div>
  <div class="field">
    <%= f.label :calorie_burn %><br>
    <%= f.number_field :calorie_burn %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
