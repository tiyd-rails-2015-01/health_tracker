<%= form_for(@calories_consumed) do |f| %>
  <% if @calories_consumed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calories_consumed.errors.count, "error") %> prohibited this calories_consumed from being saved:</h2>

      <ul>
      <% @calories_consumed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :food %><br>
    <%= f.text_field :food %>
  </div>
  <div class="field">
    <%= f.label :calories %><br>
    <%= f.number_field :calories %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
