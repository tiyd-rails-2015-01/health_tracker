<%= form_for(@joule) do |f| %>
  <% if @joule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@joule.errors.count, "error") %> prohibited this joule from being saved:</h2>

      <ul>
      <% @joule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :consumed_calories %><br>
    <%= f.number_field :consumed_joules %>
  </div>
  <div class="field">
    <%= f.label :added_on %><br>
    <%= f.date_select :added_on %>
  </div>
  <div class="actions">
    <%= f.submit("Add New Calories") %>
  </div>
<% end %>
