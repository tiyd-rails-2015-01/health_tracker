<%= form_for(@weight) do |f| %>
  <% if @weight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weight.errors.count, "error") %> prohibited this weight from being saved:</h2>

      <ul>
      <% @weight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :weight_amount %><br>
    <%= f.text_field :weight_amount %>
  </div>
  <div class="field">
    <%= f.label :weight_on %><br>
    <%= f.date_select :weight_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
